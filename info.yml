---
- name: Get info about "{{ variable_host }}"
  hosts: "{{ variable_host }}"
  gather_facts: false
  pre_tasks:
    - import_tasks: common/tasks/init_facts.yml
      vars:
        is_chroot_param: False
  roles:
    - get_info
