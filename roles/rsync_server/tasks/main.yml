---
- name: Install rsync package
  pkg_manager:
    name: rsync
  become: true
  become_user: "{{ install_user }}"

- name: Enable rsyncd service
  service:
    name: rsyncd.service
    enabled: yes
  become: true
